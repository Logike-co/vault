se debe instalar guest additions, menu dispositivos  ... insertar imagen de CD de los complementos del invitado..

aceptar la ejecucion del instalador

press return to close the console

create the folder in linux

mkdir /home/dev/tmp
sudo add user $USER vboxsf

add the shared folder in virtual machine options

sudo mount -t vboxsf tmp /home/dev/tmp

check the folder

